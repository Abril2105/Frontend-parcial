import React from 'react';
import ReactDOM from 'react-dom/client';
import { LibrosApp } from './LibroApp';
import 'bootstrap/dist/css/bootstrap.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <LibrosApp/>
    </React.StrictMode> 
)